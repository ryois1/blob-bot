/* eslint-disable no-irregular-whitespace */
module.exports = {
	name: 'ip',
	description: 'minecraft server ip',
	async execute(message) {
		const phrases = Array(`\`\`\` creative-mc.blob.rocks|survival-mc.blob.rocks|mc.asspoop.com:25566`,		const susmessage = phrases[Math.floor(Math.random() * phrases.length)];
		message.channel.send(susmessage);
	},
};
